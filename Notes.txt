*Built in Modules in node
-OS
-PATH
-FS
-HTTP


--OS modules
    provides built in properties and function to interacting with Operating System

--PATH modules
    to interact with files paths easily on to the System

--File System Modules (FS)
synchronous way -
    use to create rewrite append the files on to the system 
    while using writeFileSync if no file is present then it creates 
    new file and if we do not use flag for append each time it will overnight
    the file with the new content provided by us 

asynchronous way-
    